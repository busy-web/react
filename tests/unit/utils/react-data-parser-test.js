import reactDataParser from 'dummy/utils/react-data-parser';
import { module, test } from 'qunit';

module('Unit | Utility | react-data-parser', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = reactDataParser();
    assert.ok(result);
  });
});
