import lookupReact from 'dummy/utils/lookup-react';
import { module, test } from 'qunit';

module('Unit | Utility | lookup-react', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = lookupReact();
    assert.ok(result);
  });
});
