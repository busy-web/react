import model from 'dummy/utils/model';
import { module, test } from 'qunit';

module('Unit | Utility | model', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = model();
    assert.ok(result);
  });
});
